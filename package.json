{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "tims",
  "version": "0.1.0",
  "description": "Time Instructor Management System",
  "main": "dist/main/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "lint": "eslint .",
    "dev": "node scripts/dev.mjs",
    "start": "npm run lint && npm run build && electron ."
  },
  "author": {
    "name": "Oscar Fernando"
  },
  "private": true,
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "electron": "^36.0.0",
    "esbuild": "0.28.2",
    "eslint": "^10.8.1",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
